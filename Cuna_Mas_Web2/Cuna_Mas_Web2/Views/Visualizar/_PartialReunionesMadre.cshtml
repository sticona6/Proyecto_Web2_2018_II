@using Cuna_Mas_Web2.Models;
@model Ranking

@{ 
    List<Reunion> Reuniones = ViewBag.Reuniones;
}

<div class="col-lg-6">
    <div class="recent-activities card">
        <div class="card-header">
            <h3 class="h4">Reuniones Pendientes</h3>
        </div>
        <div class="card-body no-padding">
            @foreach (var item in Reuniones)
            {
                if (item.estado.Equals("A"))
                {
                    <div class="item">
                        <div class="row">
                            <div class="col-4 date-holder text-right">
                                <div class="icon"><i class="icon-clock"></i></div>
                                <div class="date"><br><span class="text-info">@(item.fecha)</span></div>
                            </div>
                            <div class="col-8 content">
                                <h5>@(item.nombre)</h5>
                                <p>@(item.descripcion)</p>
                            </div>
                        </div>
                    </div>
                }
            }
        </div>
    </div>
</div>